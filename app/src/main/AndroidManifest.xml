<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.findbooks">

    <!-- This app uses Google Books API available on the internet-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/find_books_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/find_books_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name="com.example.android.findbooks.activity.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.android.findbooks.activity.MainActivity"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>